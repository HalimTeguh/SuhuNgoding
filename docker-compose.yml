version: '3.8'

services:
  laravel:
    build:
      context: .
    volumes:
      - .:/var/www
    ports:
      - "8000:8000"
    depends_on:
      - converter

  converter:
    build:
      context: ./docker/converter
    volumes:
      - ./storage/app:/app
